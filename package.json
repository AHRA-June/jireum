{
  "name": "jireumsin-simsagi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "지름신 심사기 — 앱인토스 미니앱",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && ait build",
    "build:web": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.5",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@apps-in-toss/devtools": "^3.0.5",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
